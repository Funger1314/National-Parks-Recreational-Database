# Data Layout

- `raw/visitation/`: optional raw CSV files from the NPS STATS portal for Workflow A.
- `recovered/`: tables exported from the recovered legacy dump for Workflow B.
- `processed/`: deterministic outputs generated by the Python ETL workflow.

This repository does not commit the original legacy dump or undisclosed third-party datasets by default.

