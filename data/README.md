# Data Layout

- `raw/visitation/`: optional raw CSV files from the NPS STATS portal for Workflow A.
- `recovered/`: tables exported from the database backup for Workflow B.
- `processed/`: deterministic outputs generated by the Python ETL workflow.

This repository does not commit the original database backup or undisclosed third-party datasets by default.
